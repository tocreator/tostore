/// ForeignKeyOperation: Foreign key operation type enum
///
/// Defines the type of operation being performed for foreign key constraint validation
enum ForeignKeyOperation {
  /// Insert operation
  insert,

  /// Update operation
  update,
}
